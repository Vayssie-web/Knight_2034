{% extends 'base.html.twig' %}

{% block title %}Coterie{% endblock %}

{% block body %}
    <h1>Coterie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coterie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ coterie.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('coterie_index') }}">back to list</a>

    <a href="{{ path('coterie_edit', {'id': coterie.id}) }}">edit</a>

    {{ include('coterie/_delete_form.html.twig') }}
{% endblock %}
